const createTask = (req: any, res: any) => {
  res.statusCode = 200;
  res.end();
};

const vote = (req: any, res: any) => {
  res.statusCode = 200;
  res.end();
};

const createUser = (req: any, res: any) => {
  res.statusCode = 200;
  res.end();
};

export { createTask, vote, createUser };
